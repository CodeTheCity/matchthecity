<%= form_for(@venue_notice) do |f| %>
  <% if @venue_notice.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@venue_notice.errors.count, "error") %> prohibited this venue_notice from being saved:</h2>

      <ul>
      <% @venue_notice.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :venue_id %><br>
    <%= f.text_field :venue_id %>
  </div>
  <div class="field">
    <%= f.label :starts %><br>
    <%= f.datetime_select :starts %>
  </div>
  <div class="field">
    <%= f.label :expires %><br>
    <%= f.datetime_select :expires %>
  </div>
  <div class="field">
    <%= f.label :message %><br>
    <%= f.text_area :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
